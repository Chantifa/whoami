export async function gameStarted(users){
    //todo users = [{user.Id, ...rest}, {user.Id, ...rest}, ...]
}

export async function gameFinished(users){
    //todo users = [{user.Id, ...rest}, {user.Id, ...rest}, ...]
}

export async function gameWon(user){
    //todo user = {user.Id, ...rest}
}